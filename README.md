# ADAPT-CLONE-
ADAPT-CLONE is a hybrid feature-based pipeline for detecting source code plagiarism and clones, Rather than relying on a single similarity signal, it fuses four complementary detection strategies - token-level, AST, PDG-based, and semantic similarity - into one classification pipeline. Provides both binary detection and type classification.
